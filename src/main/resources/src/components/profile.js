import React from 'react';
import {BrowserRouter as Router, Switch, Route, Link} from "react-router-dom"

function Profile(){
    return(
        <Router>
            <h1>Esta en el perfil</h1>
        </Router>
    )
}

export default Profile;